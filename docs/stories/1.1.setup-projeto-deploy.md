# Story 1.1: Setup do Projeto e Deploy Base

## Status
Done

## Executor Assignment
```
executor: "@devops"
quality_gate: "@dev"
quality_gate_tools: ["lint", "type-check", "build"]
```

## Story
**As a** desenvolvedor,
**I want** configurar o repositório Next.js com TypeScript, Tailwind, Supabase e CI/CD,
**so that** a fundação técnica do produto esteja pronta para desenvolvimento de todas as histórias seguintes.

## Acceptance Criteria
1. Repositório Next.js 15 (App Router) criado com TypeScript e Tailwind CSS configurados e funcionando
2. Supabase projeto criado com variáveis de ambiente configuradas no `.env.local` (template em `.env.example`)
3. Rota de health-check `GET /api/health` retornando `{ status: "ok", timestamp: "ISO string" }` com status 200
4. Deploy automático na Vercel configurado: branch `main` → produção, branch `develop` → preview
5. GitHub Actions com pipeline rodando lint + type-check + build em todo PR aberto para `main` ou `develop`
6. README.md com instruções completas de setup local (pré-requisitos, variáveis de ambiente, comandos)
7. Estrutura de pastas base criada conforme `docs/framework/source-tree.md`
8. ESLint + Prettier configurados com regras base do projeto

## Tasks / Subtasks

- [ ] Inicializar projeto Next.js 15 com TypeScript (AC: 1)
  - [ ] `npx create-next-app@latest . --typescript --tailwind --app --src-dir --import-alias "@/*"`
  - [ ] Verificar que `src/app/page.tsx` e `src/app/layout.tsx` existem e compilam
  - [ ] Rodar `npm run dev` e confirmar que abre no browser

- [ ] Configurar Supabase (AC: 2)
  - [ ] Criar projeto no Supabase (supabase.com) — anotar URL e anon key
  - [ ] Instalar `@supabase/supabase-js` e `@supabase/ssr`
  - [ ] Criar `src/lib/supabase/client.ts` (browser client)
  - [ ] Criar `src/lib/supabase/server.ts` (server client com cookies)
  - [ ] Criar `.env.local` com `NEXT_PUBLIC_SUPABASE_URL` e `NEXT_PUBLIC_SUPABASE_ANON_KEY`
  - [ ] Criar `.env.example` com os mesmos campos sem valores

- [ ] Criar rota de health-check (AC: 3)
  - [ ] Criar `src/app/api/health/route.ts`
  - [ ] Retornar JSON `{ status: "ok", timestamp: new Date().toISOString() }`
  - [ ] Testar com `curl http://localhost:3000/api/health`

- [ ] Configurar estrutura de pastas (AC: 7)
  - [ ] Criar pastas: `src/components/ui/`, `src/components/workout/`, `src/components/progress/`, `src/components/onboarding/`
  - [ ] Criar pastas: `src/lib/ai/`, `src/lib/payments/`, `src/lib/notifications/`
  - [ ] Criar pastas: `src/hooks/`, `src/stores/`, `src/types/`, `src/utils/`
  - [ ] Criar `src/types/index.ts` com exports placeholder
  - [ ] Criar pastas de rotas: `src/app/(auth)/`, `src/app/(onboarding)/`, `src/app/(app)/`

- [ ] Configurar ESLint + Prettier (AC: 8)
  - [ ] Instalar `prettier eslint-config-prettier`
  - [ ] Criar `.prettierrc` com: `{ "semi": false, "singleQuote": true, "tabWidth": 2, "trailingComma": "es5" }`
  - [ ] Atualizar `eslint.config.mjs` para incluir regras TypeScript strict
  - [ ] Adicionar script `"format": "prettier --write src/**/*.{ts,tsx}"` no `package.json`

- [ ] Configurar GitHub Actions CI (AC: 5)
  - [ ] Criar `.github/workflows/ci.yml`
  - [ ] Jobs: `lint` (eslint), `typecheck` (tsc --noEmit), `build` (next build)
  - [ ] Configurar para rodar em PRs para `main` e `develop`
  - [ ] Adicionar secrets necessários no repositório GitHub (SUPABASE vars para build)

- [ ] Configurar deploy na Vercel (AC: 4)
  - [ ] Conectar repositório na Vercel
  - [ ] Configurar variáveis de ambiente na Vercel (produção + preview)
  - [ ] Confirmar que push para `main` faz deploy automático
  - [ ] Confirmar que push para `develop` cria deploy de preview

- [ ] Escrever README.md (AC: 6)
  - [ ] Seção: Pré-requisitos (Node 20+, npm, Supabase CLI)
  - [ ] Seção: Variáveis de ambiente (copiar `.env.example` → `.env.local`)
  - [ ] Seção: Comandos (`npm install`, `npm run dev`, `npm run lint`, `npm run build`)
  - [ ] Seção: Estrutura do projeto (link para `docs/framework/source-tree.md`)

## Dev Notes

### Stack e Versões
- **Next.js**: 15 com App Router (não Pages Router)
- **TypeScript**: strict mode habilitado
- **Tailwind CSS**: v4 (nova sintax — sem `tailwind.config.js`, usa `@import "tailwindcss"` no CSS global)
- **Supabase**: usar `@supabase/ssr` para Next.js (não o cliente antigo)

### Supabase Client Pattern
```typescript
// src/lib/supabase/client.ts — USO: componentes React (Client Components)
import { createBrowserClient } from '@supabase/ssr'
export function createClient() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  )
}

// src/lib/supabase/server.ts — USO: Server Components, API Routes, Server Actions
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'
export async function createClient() {
  const cookieStore = await cookies()
  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    { cookies: { getAll() { return cookieStore.getAll() }, setAll(cs) { try { cs.forEach(({ name, value, options }) => cookieStore.set(name, value, options)) } catch {} } } }
  )
}
```

### Health Check Route
```typescript
// src/app/api/health/route.ts
import { NextResponse } from 'next/server'
export async function GET() {
  return NextResponse.json({ status: 'ok', timestamp: new Date().toISOString() })
}
```

### Variáveis de Ambiente Necessárias
```bash
# .env.local (nunca commitar)
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_ROLE_KEY=eyJ...   # apenas server-side, nunca expor no client
```

### Route Groups no App Router
```
src/app/
  (auth)/          → páginas sem header/nav: /login, /cadastro, /recuperar-senha
  (onboarding)/    → páginas de onboarding: /quiz, /nivel, /local, /equipamentos
  (app)/           → área autenticada com layout: /dashboard, /workout, /progress, /settings
  api/             → API routes
```

### .gitignore mínimo para incluir
```
.env*.local
.next/
node_modules/
```

### Testing
- Nesta story, testes são manuais (verificar health-check, verificar CI passando)
- A partir da Story 1.3, escrever testes unitários para funções de banco
- Framework: Jest + Testing Library (configurar na Story 1.3)

## Change Log
| Data | Versão | Descrição | Autor |
|---|---|---|---|
| 2026-02-26 | 1.0 | Criação da story | Orion (AIOS) |
