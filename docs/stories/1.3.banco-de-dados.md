# Story 1.3: Estrutura do Banco de Dados

## Status
Done

## Executor Assignment
```
executor: "@dev"
quality_gate: "@dev"
quality_gate_tools: ["migration-test", "rls-test", "type-check"]
```

## Story
**As a** sistema,
**I want** ter o schema completo do banco de dados criado, documentado e com segurança configurada,
**so that** todos os dados do produto sejam armazenados de forma consistente e segura desde o início.

## Acceptance Criteria
1. Todas as tabelas definidas na arquitetura criadas via migration SQL versionada
2. Row Level Security (RLS) habilitado em todas as tabelas — usuário acessa apenas seus próprios dados
3. Tipos TypeScript gerados automaticamente a partir do schema do Supabase
4. Seed com catálogo inicial de equipamentos padrão (mínimo 30 equipamentos em 5 categorias)
5. Seed com catálogo inicial de conquistas/achievements (mínimo 15 badges)
6. Migration pode ser aplicada em ambiente local via `supabase db push`
7. Tabela `user_profiles` criada com trigger automático ao novo usuário se cadastrar no Auth
8. Índices criados nas colunas de busca frequente

## Tasks / Subtasks

- [ ] Instalar e configurar Supabase CLI (AC: 6)
  - [ ] `npm install -D supabase`
  - [ ] `npx supabase init` na raiz do projeto
  - [ ] `npx supabase login` e linkar com o projeto remoto
  - [ ] Criar `supabase/migrations/` para as migrations

- [ ] Migration 001: Tabelas de usuário (AC: 1, 2, 7)
  - [ ] Criar `supabase/migrations/20260226001_user_profiles.sql`
  - [ ] Tabela `user_profiles` com todos os campos da anamnese
  - [ ] Trigger `handle_new_user()` que cria registro em `user_profiles` ao criar usuário no Auth
  - [ ] RLS policies em `user_profiles`

- [ ] Migration 002: Catálogo de equipamentos (AC: 1, 2)
  - [ ] Criar `supabase/migrations/20260226002_equipment.sql`
  - [ ] Tabela `equipment_catalog` (catálogo padrão, readonly para usuários)
  - [ ] Tabela `user_equipment` (equipamentos do usuário)
  - [ ] RLS policies

- [ ] Migration 003: Exercícios (AC: 1, 2)
  - [ ] Criar `supabase/migrations/20260226003_exercises.sql`
  - [ ] Tabela `exercises` (banco de exercícios da Lets Train)
  - [ ] RLS: apenas leitura para usuários autenticados

- [ ] Migration 004: Treinos e avaliações (AC: 1, 2)
  - [ ] Criar `supabase/migrations/20260226004_workouts.sql`
  - [ ] Tabela `workouts` com campo `exercicios JSONB`
  - [ ] Tabela `workout_evaluations`
  - [ ] Índice em `workouts(user_id, data)` para busca por histórico
  - [ ] RLS policies

- [ ] Migration 005: Progressão e gamificação (AC: 1, 2)
  - [ ] Criar `supabase/migrations/20260226005_progress.sql`
  - [ ] Tabela `user_progress` com trigger para atualizar ao concluir treino
  - [ ] Tabela `achievements` (catálogo de conquistas)
  - [ ] Tabela `user_achievements`
  - [ ] RLS policies

- [ ] Migration 006: Assinaturas e notificações (AC: 1, 2)
  - [ ] Criar `supabase/migrations/20260226006_subscriptions.sql`
  - [ ] Tabela `subscriptions`
  - [ ] Tabela `push_subscriptions`
  - [ ] Índice em `subscriptions(user_id, status)`
  - [ ] RLS policies

- [ ] Seed: Catálogo de equipamentos (AC: 4)
  - [ ] Criar `supabase/seed.sql`
  - [ ] Inserir 30+ equipamentos nas 5 categorias:
    - Pesos Livres: halter, barra, barra EZ, kettlebell, anilhas, halteres fixos
    - Máquinas: leg press, cadeira extensora, cadeira flexora, peck deck, pulley, smith machine, chest press, shoulder press, lat pulldown
    - Cabos: cabo crossover, cabo baixo, cabo alto, polia dupla
    - Cardio: esteira, bicicleta ergométrica, elíptico, remo ergométrico, corda de pular
    - Funcionais: colchonete, bola suíça, elástico, TRX, step, cones, medicine ball

- [ ] Seed: Conquistas iniciais (AC: 5)
  - [ ] Inserir 15+ achievements:
    - "Primeiro Passo" (1 treino), "Semana 1" (7 treinos), "Dedicado" (30 treinos)
    - "Centurião" (100 treinos), "Consistente" (streak 7 dias), "Imparável" (streak 30 dias)
    - "Iniciante" (level up para iniciante), "Intermediário", "Avançado", "PRO"
    - "Madrugador" (treino antes das 7h), "Noturno" (treino após 21h)
    - "Explorador" (usar 10 exercícios diferentes), "Diversificado" (treinar 3 tipos diferentes)
    - "Avaliador" (avaliar 10 treinos)

- [ ] Gerar tipos TypeScript (AC: 3)
  - [ ] `npx supabase gen types typescript --project-id [ID] > src/types/database.types.ts`
  - [ ] Criar `src/types/index.ts` re-exportando tipos úteis
  - [ ] Adicionar script no `package.json`: `"db:types": "supabase gen types typescript --project-id [ID] > src/types/database.types.ts"`

## Dev Notes

### Migration 001 — user_profiles completo
```sql
-- supabase/migrations/20260226001_user_profiles.sql
CREATE TABLE public.user_profiles (
  id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  nome TEXT,
  peso DECIMAL(5,2),
  idade INTEGER,
  altura DECIMAL(5,2),
  tempo_sem_treinar TEXT CHECK (tempo_sem_treinar IN ('menos_6_meses', '6m_1ano', 'mais_1_ano', 'nunca_treinou')),
  doenca_cardiaca BOOLEAN DEFAULT false,
  lesao_cronica BOOLEAN DEFAULT false,
  lesao_descricao TEXT,
  medicamento_controlado BOOLEAN DEFAULT false,
  objetivo TEXT CHECK (objetivo IN ('perda_peso', 'ganho_massa', 'qualidade_vida')),
  dias_por_semana INTEGER CHECK (dias_por_semana BETWEEN 2 AND 6),
  preferencia_treino TEXT CHECK (preferencia_treino IN ('isolados', 'grupos_musculares', 'superior_inferior')),
  nivel_atual TEXT DEFAULT 'adaptacao' CHECK (nivel_atual IN ('adaptacao', 'iniciante', 'intermediario', 'avancado', 'pro')),
  local_treino TEXT CHECK (local_treino IN ('condominio', 'academia')),
  onboarding_completo BOOLEAN DEFAULT false,
  onboarding_etapa TEXT DEFAULT 'quiz',
  criado_em TIMESTAMPTZ DEFAULT NOW(),
  atualizado_em TIMESTAMPTZ DEFAULT NOW()
);

-- Trigger: criar user_profile ao cadastrar
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS TRIGGER AS $$
BEGIN
  INSERT INTO public.user_profiles (id)
  VALUES (NEW.id);
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE FUNCTION public.handle_new_user();

-- RLS
ALTER TABLE public.user_profiles ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Users can view own profile" ON public.user_profiles FOR SELECT USING (auth.uid() = id);
CREATE POLICY "Users can update own profile" ON public.user_profiles FOR UPDATE USING (auth.uid() = id);
```

### Estrutura JSONB dos exercícios no treino
```typescript
// Estrutura do campo workouts.exercicios (JSONB)
interface WorkoutExercise {
  nome: string
  grupo_muscular: string[]
  series: number
  repeticoes: string      // ex: "12", "8-12", "falha"
  descanso_segundos: number
  youtube_url?: string
  instrucoes?: string
  ordem: number
}
interface WorkoutData {
  aquecimento: WorkoutExercise[]
  principal: WorkoutExercise[]
  cooldown: WorkoutExercise[]
}
```

### Níveis e Requisitos de Progressão
```sql
-- Inserir como tabela de configuração ou constante no código
-- Adaptação → Iniciante: 20 treinos
-- Iniciante → Intermediário: 40 treinos
-- Intermediário → Avançado: 60 treinos
-- Avançado → PRO: 80 treinos
```

### RLS Pattern para todas as tabelas
```sql
-- Padrão a seguir em todas as tabelas com user_id
ALTER TABLE public.[tabela] ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Users can manage own data" ON public.[tabela]
  USING (auth.uid() = user_id)
  WITH CHECK (auth.uid() = user_id);
```

### Arquivos a Criar
```
supabase/
  migrations/
    20260226001_user_profiles.sql
    20260226002_equipment.sql
    20260226003_exercises.sql
    20260226004_workouts.sql
    20260226005_progress.sql
    20260226006_subscriptions.sql
  seed.sql
src/
  types/
    database.types.ts   (gerado pelo CLI)
    index.ts
```

### Comandos Úteis
```bash
npx supabase db push                    # aplica migrations locais no remoto
npx supabase db reset                   # reseta DB local e re-aplica tudo
npx supabase gen types typescript ...   # regenera tipos TypeScript
```

### Dependências
Nenhuma nova dependência npm necessária — Supabase CLI já foi instalado como devDependency.

### Testing
- Aplicar migrations em ambiente local (`supabase db push`)
- Verificar no Supabase Dashboard que todas as tabelas existem
- Testar RLS: como usuário A, tentar acessar dados do usuário B (deve retornar vazio ou 403)
- Verificar que trigger `handle_new_user` cria `user_profiles` ao cadastrar novo usuário
- Validar tipos TypeScript gerados compilam sem erros

## Change Log
| Data | Versão | Descrição | Autor |
|---|---|---|---|
| 2026-02-26 | 1.0 | Criação da story | Orion (AIOS) |
